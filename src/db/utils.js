const compareDates = (date) => {
    const currentDate = new Date()

    return( 
    `${date.getMonth()}/${date.getFullYear()}` == `${currentDate.getMonth()}/${currentDate.getFullYear()}`
    )
}

const newVisualization = (element) => {

    
}


newVisualization()